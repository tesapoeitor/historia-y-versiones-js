const hello = 'Hello'
const world = 'World'
const epicPhrase1 = hello + ' ' + world + '!'
console.log(epicPhrase1)

// Template literals
const epicPhrase2 = `${hello} ${world}!`
console.log(epicPhrase2)

// Multi lines
const text =`Esta es una frase
    en multiples líneas`
console.log(text)